---
title: Core Package
description: Notes about the Facilis core package workspace.
---

The `facilis` package lives in `packages/facilis`.

For now it remains intentionally minimal so the repository can establish its
workspace structure before core implementation work begins.
