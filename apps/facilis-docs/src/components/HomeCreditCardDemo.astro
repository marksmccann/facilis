<div class="home-credit-card-demo">
    <label class="home-credit-card-demo__label" for="home-credit-card">
        Credit card
    </label>
    <input
        class="home-credit-card-demo__input"
        id="home-credit-card"
        data-home-credit-card
        inputmode="numeric"
        placeholder="Try 4242abcd42424242"
        type="text"
    />
    <p class="home-credit-card-demo__note">
        Messy input like <code>4242abcd42424242</code> becomes
        <code>4242 4242 4242 4242</code>.
    </p>
</div>

<script>
    import { mountHomeCreditCardDemo } from '../demo/mountHomeCreditCardDemo.ts';

    mountHomeCreditCardDemo();
</script>

<style>
    .home-credit-card-demo {
        display: grid;
        gap: 0.75rem;
        max-width: 30rem;
        margin-block: 1.25rem 1.5rem;
        padding: 1rem;
        border: 1px solid var(--sl-color-gray-5);
        border-radius: 0.5rem;
        background: var(--sl-color-black);
    }

    .home-credit-card-demo__label {
        color: var(--sl-color-white);
        font-size: var(--sl-text-sm);
        font-weight: 650;
    }

    .home-credit-card-demo__input {
        width: 100%;
        padding: 0.75rem 0.85rem;
        border: 1px solid var(--sl-color-gray-5);
        border-radius: 0.4rem;
        background: var(--sl-rapide-ui-bg-color);
        color: var(--sl-color-white);
        font: inherit;
        font-size: var(--sl-text-lg);
    }

    .home-credit-card-demo__input:focus {
        border-color: var(--sl-color-accent);
        outline: 2px solid var(--sl-color-accent-low);
        outline-offset: 2px;
    }

    .home-credit-card-demo__input::placeholder {
        color: var(--sl-color-gray-3);
    }

    .home-credit-card-demo__note {
        margin: 0;
        color: var(--sl-color-gray-2);
        font-size: var(--sl-text-sm);
        line-height: 1.5;
    }
</style>
